<?php
$pname_err = "";
$price_err ="";
$application_err = "";
$material_err ="";
$kind_err ="";
$pic_err = "";
/*$fill="لطفا تکمیل نمایید";
if ($_SERVER["REQUEST_METHOD"] == "POST") {
   $firstname = checkInput($_POST["firstname"]);
   if(empty($firstname)) $firstname_err=$fill;
   echo $firstname;
   
   
   $email=$_POST['email'];
   $emailPattern = "/[a-zA-Z0-9_\-.+]+@[a-zA-Z0-9-]+.[a-zA-Z]+/";
   if(!preg_match($emailPattern, $email)) $email_err='لطفا ایمیل صحیح وارد کنید';

}

function checkInput($data) {
   $data = trim($data); // remove unnecessary characters: extra space, tab, newline
   $data = stripslashes($data); // Remove backslash "\"
   $data = htmlspecialchars($data); // behave code as HTML escaped code e.g., <script>  ->   &lt;script&gt;
   return $data;
}*/
?>

